import { SatelliteTypeCount } from './satellite-type-count';

describe('SatelliteTypeCount', () => {
  it('should create an instance', () => {
    expect(new SatelliteTypeCount()).toBeTruthy();
  });
});
